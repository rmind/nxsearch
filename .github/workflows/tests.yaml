name: Test
on: [push]

jobs:
  build_and_test:
    name: Build and test
    runs-on: ubuntu-latest
    steps:
      - name: Check out the repo
        uses: actions/checkout@v3

      - name: Checkout submodules
        run: git submodule update --init --recursive

      - name: build
        shell: bash
        run: docker build -f compose/nxsearch.Dockerfile .
